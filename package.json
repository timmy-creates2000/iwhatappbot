{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"api,app\" --prefix-colors \"green,cyan\" \"pnpm --filter @workspace/api-server run dev\" \"cross-env PORT=3000 BASE_PATH=/ pnpm --filter @workspace/app run dev\"",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "dev:app": "cross-env PORT=3000 BASE_PATH=/ pnpm --filter @workspace/app run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^10.1.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
